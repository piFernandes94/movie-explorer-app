import { CircularProgress } from "@mui/material"

const Loader = () => {
    return (
        <article className="loader">
            <CircularProgress />
        </article>
    )
}

export default Loader